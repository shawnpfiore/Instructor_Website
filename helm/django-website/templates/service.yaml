apiVersion: v1
kind: Service
metadata:
  name: {{ include "django-website.fullname" . }}
  labels:
    {{- include "django-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "django-website.selectorLabels" . | nindent 4 }}
